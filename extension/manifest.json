{
  "manifest_version": 3,
  "name": "Intake AI — eSource Build Agent",
  "version": "0.1.0",
  "description": "Agent that builds a clinical trial study inside any eSource platform from a structured input file, with a human-in-the-loop gate.",
  "permissions": [
    "debugger",
    "storage",
    "sidePanel",
    "activeTab",
    "scripting"
  ],
  "host_permissions": [
    "https://api.groq.com/*"
  ],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  },
  "side_panel": {
    "default_path": "sidepanel.html"
  },
  "options_page": "options.html",
  "action": {
    "default_title": "Intake AI Agent"
  },
  "commands": {
    "grant-page-access": {
      "suggested_key": {
        "default": "Ctrl+Shift+K",
        "mac": "Command+Shift+K"
      },
      "description": "Grant the agent temporary access to the active tab (needed before a run, since opening the side panel alone does not grant activeTab)"
    }
  }
}